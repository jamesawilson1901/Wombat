package com.wombat.split.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette — sampled from the launcher icon: silver line-art on near-black.
// The icon sets the brand; these values are fixed and never replaced by
// Material You dynamic colour.

// Neutrals (dark)
val NearBlack = Color(0xFF16161D)      // icon tile background (sampled from source art)
val Graphite = Color(0xFF1E1E26)
val GraphiteHigh = Color(0xFF26262F)
val Silver = Color(0xFFCCCCCE)         // icon line colour (sampled from source art)
val SilverBright = Color(0xFFE5E5E9)
val SilverDim = Color(0xFF9B9CA4)
val OutlineDark = Color(0xFF45464E)
val OutlineVariantDark = Color(0xFF2E2E37)

// Neutrals (light) — same silver-on-neutral character, inverted
val Porcelain = Color(0xFFF5F5F7)
val PorcelainHigh = Color(0xFFFDFDFE)
val InkGrey = Color(0xFF1B1B1F)
val GraphiteMid = Color(0xFF55565E)
val SilverContainerLight = Color(0xFFE3E3E9)
val OutlineLight = Color(0xFF75767D)
val OutlineVariantLight = Color(0xFFC5C6CD)

// Error (standard Material 3 error tones — kept, they read fine on both palettes)
val ErrorDark = Color(0xFFFFB4AB)
val OnErrorDark = Color(0xFF690005)
val ErrorContainerDark = Color(0xFF93000A)
val OnErrorContainerDark = Color(0xFFFFDAD6)
val ErrorLight = Color(0xFFBA1A1A)
val OnErrorLight = Color(0xFFFFFFFF)
val ErrorContainerLight = Color(0xFFFFDAD6)
val OnErrorContainerLight = Color(0xFF410002)
